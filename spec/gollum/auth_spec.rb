require "spec_helper"

RSpec.describe Gollum::Auth do
  it "has a version number" do
    expect(Gollum::Auth::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
